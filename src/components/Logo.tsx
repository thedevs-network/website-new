/** @jsx jsx */
import { jsx, Box } from "theme-ui";
import { FC } from "react";

const Logo: FC = () => {
  return (
    <Box
      sx={{
        color: "rgba(255, 255, 255, 0.5)",
        fontSize: 4,
        letterSpacing: 0,
        fontWeight: "body",
      }}
      as="p"
      // TODO: label
    >
      //
    </Box>
  );
};

export default Logo;
