/** @jsx jsx */
import { jsx, Box, Link, Flex } from "theme-ui";
import { Section } from "./Layout";

const Footer = () => {
  return (
    <Section styles={{ backgroundColor: "backgroundFooter" }}>
      <Flex sx={{ py: 5 }}>ayyyy</Flex>
    </Section>
  );
};

export default Footer;
